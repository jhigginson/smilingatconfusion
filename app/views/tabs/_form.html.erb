<%= simple_form_for @tab do |f| %>
  <%= f.input :title %>
  <%= f.association :artist, include_blank: false %>
  <%= link_to new_artist_path, remote: true, class: "btn btn-info",id: :add_artist_link do%>
    <i class="icon-plus"></i>
  <% end %>
  <br />
  <br />
  *<%= f.association :album %>
  <%= link_to new_album_path, remote: true, class: "btn btn-info",id: :add_album_link do%>
    <i class="icon-plus"></i>
  <% end %>
  <br />
  <br />
  <%= f.association :tuning, label_method: :display, include_blank:false%>
  <%= link_to new_tuning_path, remote: true, class: "btn btn-info",id: :add_tuning_link do%>
    <i class="icon-plus"></i>
  <% end %>
  <br />
  <br />
  <%= f.input :notes, input_html: { class: 'wide_text_area' } %>
  <%= f.input :body, input_html: { class: 'tab_body wide_text_area', rows: 50 } %>
  <%= f.button :submit, class: 'btn-primary' %>
<% end %>
